import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        paddingTop: 4,
        paddingBottom: 4,
        borderRadius: 5
    },
    buttonText: {
        color: 'white',
        textAlign: 'center',
        fontFamily: 'montserrat-bold'
    },
});